#include <stdio.h>
#include <stdlib.h>

void CargarSecuenciaDelArray( int miArray[], int cantidad, char mensaje[]);
void CargarSecuenciaDelArray( int miArray[], int cantidad, char mensaje[]);
{
    int listadoDeEdades [10];
    int i;
   for ( i = 0; i < 10; i++)
    {
        printf("Ingrese la edad:");
        scanf("%d", &listadoDeEdades[i]);
    }
}
